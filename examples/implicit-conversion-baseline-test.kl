#lang "implicit-conversion-baseline.kl"

(example
  42)

(example
  (let-implicit string-length
    (+ 1 "foo")))

--(example
--  (let-implicit string-length
--    (let-implicit integer->string
--      (string-append (+ 1 "foo") "!"))))
--
--(example
--  (lambda ((the Integer x))
--    (+ 2 x)))
--
--(example
--  (let-implicit (lambda ((the String s))
--                  (* 10 (string-length s)))
--    (let-implicit integer->string
--      (string-append (+ 1 "foo") "!"))))
