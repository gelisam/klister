#lang "prelude.kl"

(datatype (OfUnit F)
  (of-unit (F Unit)))

(example
  (the (OfUnit (IO))
       (of-unit (pure-IO unit))))
(example
  (the (OfUnit (Maybe))
       (of-unit (just unit))))

(export OfUnit of-unit)
